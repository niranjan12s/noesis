In the documents tab, we also allow multi file upload, say they can upload 5 files max, which will be triggering our ingestion pipleline , alsoo I want a bulk indexing mode for our application , where we start our application in bulk indexing mode, where the UI calls it out as Bulk Indexing mode vs realtime mode, where our entire application basically maximises for throughput, scales aggresively for chunk extraction and edge indexing with temporarily longer refresh intervals for opensearch , bulk APIs, and larger batches . we disable querying during this mode, and the UI allows us to make this transition maybe  additionally the ingestion mdoe changes , we basically ask the user to choose a directory to watch , and once chosen, all files added to that folder are picked for indexing,  the UI should also switch to showing bulk ingestion relevant information, Ingestion overview should consist of total files discovered, files processed, files failed, assertions geenrated, edges geneerated, queue backlog, ingestion throughput, ETA for finishing also a loading bar that shows progress visibly also show pipeline health, we should show these metrics changing overtime, like documents processsed per second over time, and also assertions /sec and edges /sec over time to know if workers saturate, if opensearch bottlenecks and if llm throttling occurs, we should allow for multiple instances of our app and use redis for heartbeats , we freeze graph interactions, also show a banner on the UI saying querying is disabled, once a bulk job starts, as in user uploads something, we do not let them turn it off until it completes, onthe graph view in the UI, we can show the graph growing with nodes and edges incrementally with + x nodes /sec , +y edges /sec on the top  we should only have a read only document tab during the bulk indexing process, we could also show information like CPU usage, RAM usage and allow for another instance of our app to be parallely working with us, we only allow for a mode change on complete restart of app, so this can be on a config file, in order to distribute work among the systems, we should be allowed to make new instances of the app , which will be sharing the same db, queeu and same redis instance but just processing documents parallely, we can have them send a heartbeat to redis for building consistent hashing on the files to allow for multiple workers to dynamically own loads , ensure that duplicates are not getting into the db, and we also show the number of workers on the dashboard, so each worker can expose worker id, instance name, version, uptime, heartbeat, and current load from which our ui should be able to show wkrokees, status of them, as in healthy otr lagging, and throughput of how many documents they are completing.
